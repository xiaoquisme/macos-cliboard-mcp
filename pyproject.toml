[project]
name = "macos-clipboard-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "applescript",
    "mcp[cli]>=1.7.1",
    "pillow>=11.2.1",
]

[tool.poetry.dependencies]
Pillow = "^10.0.0"
